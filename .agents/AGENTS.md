# Agent Rules

- **Workspace File Lock**: Do NOT make any changes or modifications to files under `QuestionBank/json/rrb_clerk/` or `QuestionBank/json/sc_gd/` (the `rrb_clerk` and `ssc_gd` courses) unless the user explicitly instructs you to edit those files.
